# Local config: holds the FTP password. Copy monitor_config.example.json to
# monitor_config.json and fill in the credentials.
monitor_config.json

# Machine-local MCP server paths
.mcp.json

# Runtime state / caches (large, machine-specific)
monitor_output.json
monitor_state.json
sync_cache.json
blackmagic_compress_state.json
*.tmp

# Logs and generated dirs
logs/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Agent scratch
.superpowers/
.claude/

# Scratch encode temp on the destination mount (never in-repo, but be safe)
.blackmagic_mini_tmp/
cc_pipeline/node_modules
