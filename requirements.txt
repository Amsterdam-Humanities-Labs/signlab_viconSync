# Runtime dependencies. Nothing here is installed by any deploy step - this
# estate has no build step - so treat it as the record of what a host needs
# rather than as something that runs.
requests>=2.25
pymysql>=1.0

# The heartbeat client and the rotating-log setup, from the repository that
# also holds the API they talk to. Optional: python_client.py beside these
# scripts is a byte-identical vendored copy, and the imports fall back to it.
# Installing it is what lets that copy eventually be deleted.
signlab-client-monitor @ git+https://github.com/Amsterdam-Humanities-Labs/signlab_client_monitor_api@main#subdirectory=client
